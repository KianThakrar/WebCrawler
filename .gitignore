# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.so
.installed.cfg

# Virtual environments
.env
.venv
env/
venv/
ENV/

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
nosetests.xml
coverage.xml
*.cover
junit*.xml

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Personal notes
VIDEO_SCRIPT.md
EVALUATION.md

# Local evaluation workspace (raw HTML used to draft relevance judgements)
.eval_workspace/

# Relevance judgements — kept local for personal review, not part of the
# repo submission.  The eval feature still ships; the judgements file is
# loaded at runtime if present.
tests/relevance_judgements.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Data — keep the directory; the compiled index.json is a submission artefact
data/*.pkl
data/*.db
!data/.gitkeep
!data/index.json
